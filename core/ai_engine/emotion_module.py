from typing import List, Dict

def extract_emotions(batch_results: List[Dict]) -> List[Dict]:
    # En un escenario real, ya vienen en el JSON del LLM.
    # Aquí simplemente passthrough desde api_call.
    return batch_results
