from typing import List, Dict

def compute_churn(batch_results: List[Dict]) -> List[Dict]:
    # Passthrough: en demo viene del LLM; en real, podrías ajustar con heurísticas.
    return batch_results
